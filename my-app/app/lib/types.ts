export type User = {
    id: string,
    username: string,
    email: string,
    password: string,
    enabled: boolean,
}

export type dbUser = {
    username: string,
    email: string,
    password: string,
    enabled: boolean,
}